tls:
  certificates:
    - certFile: "/etc/certs/fullchain.cer"
      keyFile: "/etc/certs/{{ cert.domain }}.key"

http:
  services:
    authentik:
      loadBalancer:
        servers:
        - url: "localhost:9000"
  routers:
    authentik:
      tls: true
      rule: "PathPrefix(`/auth`)"
      service: authentik